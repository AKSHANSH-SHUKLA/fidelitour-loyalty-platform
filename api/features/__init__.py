# FidéliTour feature modules — additive only.
# Each module in this package is self-contained and registers its own
# APIRouter via _loader.register_all(app, db). Adding or removing a
# module never affects existing endpoints, models, or pages.
